#!/bin/bash
# This file is no longer used.
# Frontend is now deployed to Vercel, which handles build process automatically.
# Vercel will run: npm install && npm run build
# Build output: dist/ directory
